import styled from 'styled-components';

// eslint-disable-next-line import/prefer-default-export
export const StyledFormSuccessMessage = styled.p`
  font-size: 15px;
  color: ${({ theme }) => theme.colors.white};
`;
